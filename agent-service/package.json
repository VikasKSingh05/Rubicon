{
  "name": "rubicon-agent-service",
  "version": "0.1.0",
  "description": "Rubicon agent-service stub — POST /agent/query (tool-grounded chat).",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
